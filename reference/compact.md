# Compact a set of cells by merging complete sibling groups into parent cells

Compact a set of cells by merging complete sibling groups into parent
cells

## Usage

``` r
compact(cells)
```

## Arguments

- cells:

  vector of cell IDs to compact.

## Value

vector of compacted cell IDs (typically smaller than input).
