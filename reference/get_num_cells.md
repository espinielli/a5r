# Get the number of cells at a given resolution

Get the number of cells at a given resolution

## Usage

``` r
get_num_cells(resolution)
```

## Arguments

- resolution:

  resolution level (0-30).

## Value

number of cells at the specified resolution.
