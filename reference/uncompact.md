# Uncompact (expand) cells to a target resolution

Uncompact (expand) cells to a target resolution

## Usage

``` r
uncompact(cells, target_resolution)
```

## Arguments

- cells:

  vector of cell IDs to expand.

- target_resolution:

  the target resolution for all output cells.

## Value

vector of cell IDs all at the target resolution.
